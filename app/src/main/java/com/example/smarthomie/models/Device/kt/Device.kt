package com.example.smarthomie.models.Device.kt
/*
class Device{
    val id: Int,                      // Immutable property (cannot change after initialization)
    var name: String,                 // Mutable property (can change after initialization)
    var type: String,
    var status: String,
    var icon: String,
    val ownerUserId: Int,             // Assuming owner won't change
    var lastUpdated: Long
}
 */